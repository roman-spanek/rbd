# Database connection settings
# These values are used when no environment variable or system property overrides them.
# db.password is intentionally empty for local development; set it before deploying.
db.url=jdbc:postgresql://localhost:5432/mydb
db.user=postgres
db.password=